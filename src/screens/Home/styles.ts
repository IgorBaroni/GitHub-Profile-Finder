import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container1: {
        flex: 1,
        backgroundColor: '#121212',
        paddingHorizontal: 8,
    },
    container: {
        alignItems: 'center',
    }
});